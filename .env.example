# ===========================================
# Environment Configuration Template
# Copy to .env.local for development
# Copy to .env.production for production
# ===========================================

# Environment (development | production)
NODE_ENV=development

# ===========================================
# SUPABASE CONFIGURATION
# ===========================================

# Supabase Project URL (different for dev/prod)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (public, safe for client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (private, server-only!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ===========================================
# DATABASE CONFIGURATION (Prisma + Supabase)
# ===========================================

# Connection pooling URL (for Prisma Client - uses pgBouncer)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# Direct connection URL (for migrations - bypasses pgBouncer)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
DIRECT_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres

# ===========================================
# SECURITY
# ===========================================

# JWT Secret for authentication tokens
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Encryption key for sensitive data (must be set, no defaults!)
ENCRYPTION_KEY=your-encryption-key-min-32-characters

# ===========================================
# STORAGE
# ===========================================

# Supabase Storage bucket names
STORAGE_BUCKET_ITEMS=items
STORAGE_BUCKET_DOCUMENTS=documents
STORAGE_BUCKET_AVATARS=avatars

# ===========================================
# OPTIONAL: OLD MONGODB (for migration only)
# ===========================================

# MONGODB_URI=mongodb://localhost:27017/arendapro

# ===========================================
# WEB PUSH (VAPID)
# ===========================================

# Generate with: node -e "const wp = require('web-push'); const v = wp.generateVAPIDKeys(); console.log(v)"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=mailto:admin@arenada.pro

# ===========================================
# YANDEX MAPS
# ===========================================

# Get key at https://developer.tech.yandex.ru/ (JavaScript API + Geocoder)
NEXT_PUBLIC_YANDEX_MAPS_API_KEY=your-yandex-maps-api-key
